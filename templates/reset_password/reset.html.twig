{% extends 'base.html.twig' %}

{% block title %}Reset your password
{% endblock %}

{% block body %}
	<h1 class="texte-center space-top" >Reset your password</h1>
	<div class="container d-flex space-top">

		<div class="d-flex justify-content-center space-top">
			{{ form_start(resetForm) }}
			{{ form_row(resetForm.plainPassword) }}
			<button class="btn btn-secondary">Reset password</button>
			{{ form_end(resetForm) }}
		</div>

	</div>

{% endblock %}
